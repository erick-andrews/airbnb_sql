# Flyway Code for airbnb_sql project

This folder contains relevant DDL code for execution by flyway through docker as defined in `flyway_migration_cli.sh`. Each version represents an update to the schema of the postgres db in a version controlled fashion

